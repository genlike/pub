{
  "private": true,
  "engines": {
    "yarn": ">=1.7.0 <2",
    "node": ">=14.18.0"
  },
  "scripts": {
    "postinstall": "theia check:theia-version",
    "rebuild:browser": "theia rebuild:browser",
    "start:browser": "yarn rebuild:browser && yarn --cwd browser-app start",
  },
  "devDependencies": {
    "lerna": "2.4.0"
  },
  "workspaces": [
    "itlingo-itoi", "browser-app", "electron-app"
  ]
}
